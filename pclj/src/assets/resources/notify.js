
(function () {

    pclj.notify.success = function (element, title, time) {
      alert('Adicione o componente <message/> no componente base do módulo...');
    };
  
    pclj.notify.info = function (message, title, time) {
      alert('Adicione o componente <message/> no componente base do módulo...');
    };
  
    pclj.notify.warn = function (element, title, time) {
      alert('Adicione o componente <message/> no componente base do módulo...');
    };
  
    pclj.notify.error = function (element, title, time) {
      alert('Adicione o componente <message/> no componente base do módulo...');
    };
  
  })();